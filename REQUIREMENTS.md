# 📋 カンタンデータクローラー 社内版開発 - 完全要件定義書

## 🎯 1. プロジェクト概要

### 1.1 目的
複数の広告プラットフォームのデータを自動的にGoogleスプレッドシートに出力し、レポート作成業務を完全自動化するSaaSツール

### 1.2 想定ユーザー
- 社内の広告運用チーム
- マーケティング部門
- データ分析チーム

### 1.3 期待効果
- データ入力作業: 1日30分×人数 の工数削減
- リアルタイムデータによる迅速な意思決定
- 外部ツール費用の削減

---

## 📐 2. 機能要件詳細

### 2.1 ユーザー認証・管理

#### 2.1.1 認証方式

**必須:**
- 社内SSO連携（SAML 2.0 / OAuth 2.0）
- または、メール + パスワード認証

**オプション:**
- 多要素認証（MFA）
- Google Workspace連携

#### 2.1.2 ユーザー情報

```typescript
interface User {
  id: string;
  email: string;
  name: string;
  created_at: timestamp;
  updated_at: timestamp;
  subscription_type: 'free' | 'starter' | 'essential' | 'pro' | 'enterprise';
  available_platform_types: string[]; // ['meta_ads', 'google_ads', ...]
}
```

#### 2.1.3 権限管理
- **管理者:** 全機能アクセス、ユーザー管理
- **一般ユーザー:** クローラー作成・編集・削除
- **閲覧者:** クローラー閲覧のみ（オプション）

---

### 2.2 クローラー管理機能

#### 2.2.1 クローラーデータモデル

```typescript
interface Crawler {
  id: string;
  name: string; // クローラー名
  user_id: string;
  platform: Platform; // 'meta_ads' | 'google_ads' | 'tiktok_ads' ...
  status: 'active' | 'inactive';
  
  // スプレッドシート設定
  spreadsheet_url: string;
  spreadsheet_id: string; // URLから抽出
  sheet_name: string;
  
  // データソース設定
  account_ids: string[]; // 広告アカウントID配列
  
  // レポート設定
  report_config: ReportConfig;
  
  // スケジュール設定
  schedule: ScheduleConfig;
  
  // その他
  tags: string[]; // 最大5つ
  created_at: timestamp;
  updated_at: timestamp;
  last_executed_at: timestamp | null;
}

interface ReportConfig {
  // 期間設定
  date_range_type: DateRangeType;
  date_range_start?: string; // 'YYYY-MM-DD' 特定日の場合
  lookback_days?: number; // 過去X日間の場合
  
  // ディメンション（分割単位）
  dimensions: string[]; // ['date', 'campaign_name', 'ad_set_name', ...]
  
  // メトリクス（指標）
  metrics: string[]; // ['impressions', 'clicks', 'spend', ...]
  
  // カスタムコンバージョン（Meta広告）
  custom_conversions?: {
    conversion_id: string;
  }[];
  
  // カスタムイベント（Meta広告）
  custom_events?: {
    event_name: string;
  }[];
  
  // フィルター
  exclude_zero_cost: boolean; // 費用0のレコード除外
}

type DateRangeType = 
  | 'today'                    // 今日
  | 'yesterday'                // 昨日
  | 'last_month'               // 先月
  | 'from_specific_to_today'   // 特定の日から今日まで
  | 'from_specific_to_yesterday' // 特定の日から昨日まで
  | 'last_x_months'            // 過去X ヶ月
  | 'last_x_months_current'    // 過去X ヶ月（当月を含む、本日を含む）
  | 'last_x_months_no_current' // 過去X ヶ月（当月を含む、本日を含まない）
  | 'last_x_days_include'      // 過去X日間（本日を含む）
  | 'last_x_days_exclude'      // 過去X日間（本日を含まない）
  | 'custom';                  // カスタム

interface ScheduleConfig {
  frequency: 'hourly' | 'daily' | 'weekly' | 'monthly';
  execution_time?: string; // 'HH:mm' 形式（日次・週次の場合）
  execution_day?: number; // 1-7 (月-日) 週次の場合
}
```

#### 2.2.2 クローラー作成フロー（6ステップ）

**ステップ0: クローラー名入力**
- バリデーション: 1-100文字、必須

**ステップ1: スプレッドシート設定**

```
1-1. Google認証（初回のみ）
- OAuth 2.0 スコープ: 
  - https://www.googleapis.com/auth/spreadsheets
  - https://www.googleapis.com/auth/drive.file
  
1-2. スプレッドシートURL入力
- フォーマット: https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/...
- バリデーション: 
  - URL形式チェック
  - アクセス権限チェック
  - 存在確認
  
1-3. シート選択
- スプレッドシート内の全シートをリスト表示
- 選択したシートのデータは上書きされる旨の警告表示
```

**ステップ2: プラットフォーム選択**

選択肢（優先度順）:
1. Google広告
2. Googleアナリティクス
3. Meta広告
4. TikTok広告
5. LINE広告
6. LINE広告（同期）
7. Yahoo広告（検索）
8. Yahoo広告（ディスプレイ）
9. Smartnews広告
10. Smartnews広告 V2
11. Microsoft広告
12. X広告
13. Facebookページインサイト（β版、営業担当に連絡必要）
14. Instagramインサイト（β版、営業担当に連絡必要）
15. Amazon Seller Central（開発中）
16. アドエビス（β版、営業担当に連絡必要）

**ステップ3: データソース選択**

```
3-1. プラットフォーム認証
- 各プラットフォームのOAuth認証フロー
- 複数アカウント追加可能
- 認証情報は暗号化して保存

3-2. 広告アカウント選択
- 認証済みアカウント配下の広告アカウント一覧を表示
- 複数選択可能（チェックボックス）
- 検索機能あり
```

**ステップ4: レポート内容設定**

Meta広告の場合のディメンション選択肢:
- 期間
- 日付
- 詳細レベル
- キャンペーン名
- キャンペーンID
- 広告セット名
- 広告セットID
- 広告名
- 広告ID
- 配信
- プラットフォーム
- 国
- 性別
- 地域
- 年齢
- 配置
- 時間帯（広告主のタイムゾーン）

Meta広告の場合のメトリクス選択肢:

**よく使う:**
- クリック（すべて）
- ユニーククリック単価（すべて）
- ユニークアウトバウンドクリックの単価
- CPM（インプレッション単価）
- CTR（すべて）
- フリークエンシー
- インプレッション
- リーチ
- 消化金額
- CPC（リンクのクリック単価）

**パフォーマンス:**
- インスタントエクスペリエンスの閲覧時間
- 投稿のエンゲージメントの単価
- アウトバウンドクリックの単価
- ThruPlayの単価
- 投稿へのエンゲージメント
- インスタントエクスペリエンスを開いたクリック数
- インスタントエクスペリエンスを開始したクリック数

**コンバージョン:**
- モバイルアプリでのウィッシュリストへの追加
- モバイルアプリの登録完了
- モバイルアプリでのコンテンツ閲覧
- モバイルアプリのチェックアウト開始
- モバイルアプリでのレベル達成
- モバイルアプリでの購入
- モバイルアプリの評価の送信
- モバイルアプリの検索
- ウェブサイトの登録完了
- リンクのクリック
- ... (多数)

**レポート期間選択肢:**
- 今日
- 昨日
- 先月
- 特定の日から今日まで
- 特定の日から昨日まで
- 過去Xヶ月
- 過去Xヶ月（当月を含む、本日を含む）
- 過去Xヶ月（当月を含む、本日を含まない）
- 過去X日間（本日を含む）
- 過去X日間（本日を含まない）
- カスタム

**カスタムコンバージョン追加:**
- カスタムコンバージョンID入力フィールド
- フォーマット例: 1362683564373566
- 複数追加可能

**カスタムイベント追加:**
- カスタムイベント名入力フィールド
- 複数追加可能

**費用0のレコード除外:**
- チェックボックス
- オンにする場合は費用の指標を選択必須
- 注意書き: 費用の指標名はプラットフォームによって異なる

**ステップ5: 更新頻度設定**

選択肢:
- 毎日 (プラン: Starter以上)
- 毎週 (プラン: Starter以上)
- 毎時 (プラン: Pro以上)

更新時間選択:
- 0時〜23時（1時間単位）
- デフォルト: 7時

**ステップ6: テスト実行・保存**

タグ設定:
- 最大5個
- オートコンプリート機能
- 新規タグ作成可能

アクション:
1. クローラーをテスト実行する（任意）
   - 実際にAPIを呼び出してスプレッドシートに書き込み
   - エラーがあれば表示
   
2. クローラーを保存する
   - バリデーション全チェック
   - 保存後はクローラー一覧に遷移

---

### 2.3 スプレッドシート連携

#### 2.3.1 Google Sheets API設定

```
必要な認証情報:
- OAuth 2.0 Client ID
- Client Secret
- Redirect URI

必要なスコープ:
- https://www.googleapis.com/auth/spreadsheets
- https://www.googleapis.com/auth/drive.readonly
```

#### 2.3.2 データ書き込み仕様

```
動作:
1. 指定されたシートの全データをクリア
2. ヘッダー行を書き込み（1行目）
3. データ行を書き込み（2行目以降）

書き込みフォーマット:
- ディメンション列 → メトリクス列の順
- 日付フォーマット: YYYY-MM-DD
- 数値: カンマ区切りなし、小数点はドット
- NULL値: 空文字列

エラー処理:
- 書き込み権限がない場合: エラー通知
- スプレッドシートが削除された場合: クローラー無効化
- API制限に達した場合: リトライ（指数バックオフ）
```

---

### 2.4 広告プラットフォームAPI連携

#### 2.4.1 Google Ads API

**必要な認証情報**
- Developer Token (申請必要)
- OAuth 2.0 Client ID/Secret
- Refresh Token
- Customer ID

**主要エンドポイント**
```
レポート取得:
POST https://googleads.googleapis.com/v15/customers/{customerId}/googleAds:searchStream

主要リソース:
- campaign
- ad_group
- ad_group_ad
- metrics (impressions, clicks, cost_micros, ...)
```

**レート制限**
- Basic access: 15,000 operations/day
- Standard access: 無制限（申請必要）

#### 2.4.2 Meta Marketing API

**必要な認証情報**
- App ID / App Secret
- Access Token (User Token → System User Token推奨)
- Ad Account ID

**主要エンドポイント**
```
インサイト取得:
GET /{ad-account-id}/insights
Parameters:
  - fields: metrics
  - breakdowns: dimensions
  - time_range: {since, until}
  - level: account | campaign | adset | ad

主要メトリクス:
- impressions
- clicks
- spend
- reach
- cpm, cpc, ctr
- actions (conversions)
```

**レート制限**
- 200 calls per hour per user
- Rate limiting headers: X-Business-Use-Case-Usage

#### 2.4.3 TikTok Ads API

**必要な認証情報**
- App ID / Secret
- Access Token
- Advertiser ID

**主要エンドポイント**
```
レポート取得:
GET /open_api/v1.3/report/integrated/get/

Parameters:
  - advertiser_id
  - report_type: BASIC | AUDIENCE | PLAYABLE_MATERIAL
  - dimensions
  - metrics
  - start_date, end_date
```

#### 2.4.4 X Ads API (Twitter)

**必要な認証情報**
- API Key / API Secret
- Access Token / Access Token Secret
- Account ID

**主要エンドポイント**
```
統計データ取得:
GET /12/stats/accounts/{account_id}

Parameters:
  - entity: CAMPAIGN | LINE_ITEM | PROMOTED_TWEET
  - metric_groups
  - start_time, end_time
  - granularity: DAY | HOUR
```

#### 2.4.5 LINE Ads API

**必要な認証情報**
- API Key（申請必要、取得に1週間程度）
- Ad Account ID

**特記事項**
- API申請フォーム: https://www.lycbiz.com/jp/manual/line-ads/other_011/
- 審査に2日〜1週間

---

### 2.5 スケジューラー機能

#### 2.5.1 ジョブ管理

```typescript
interface Job {
  id: string;
  crawler_id: string;
  status: 'pending' | 'running' | 'completed' | 'failed';
  scheduled_at: timestamp;
  started_at: timestamp | null;
  completed_at: timestamp | null;
  error_message: string | null;
  retry_count: number;
}
```

#### 2.5.2 実行ロジック

1. スケジュールされた時刻にジョブをキューに追加
2. ワーカーがジョブを取得
3. 広告プラットフォームAPIからデータ取得
4. データ変換・集計
5. Googleスプレッドシートに書き込み
6. ジョブステータス更新
7. エラー時はリトライ（最大3回、指数バックオフ）

#### 2.5.3 推奨実装

**オプション1: BullMQ + Redis**
- Node.js向け
- ジョブキュー、スケジューリング、リトライ機能内蔵
- ダッシュボードあり

**オプション2: Celery + Redis**
- Python向け
- 成熟したエコシステム
- Celery Beat for periodic tasks

**オプション3: AWS EventBridge + Lambda**
- サーバーレス
- スケールしやすい
- インフラ管理不要

---

## 🎨 3. UI/UX設計

### 3.1 画面一覧

#### 3.1.1 ホーム（クローラー一覧）

```
レイアウト:
+----------------------------------+
| ヘッダー (ロゴ、メニュー、ユーザー) |
+----------------------------------+
| クローラー一覧                      | + 新規クローラー追加
+----------------------------------+
| [クローラーカード1]                |
|   - クローラー名                   |
|   - プラットフォーム                |
|   - ステータス (アクティブ/非アクティブ) |
|   - アクション:                    |
|     * スプレッドシートを開く         |
|     * 複製して作成                 |
|     * 編集する                    |
|     * 削除                       |
+----------------------------------+
| [クローラーカード2]                |
+----------------------------------+
...

フィルター:
- プラットフォーム別
- ステータス別
- タグ別
- 検索（クローラー名）
```

#### 3.1.2 新規クローラー作成

```
ステップインジケーター:
[1] → [2] → [3] → [4] → [5] → [6]

各ステップごとにフォーム表示
「次へ」「戻る」ボタン
最後のステップで「テスト実行」「保存」
```

#### 3.1.3 クローラー編集

```
新規作成と同じフォーム
既存値が入力済み
「クローラーをテスト実行する」「クローラーを更新する」
```

#### 3.1.4 アカウント設定

- メールアドレス表示
- プラン表示
- プラン変更ボタン
- 利用プラットフォーム管理
- Googleスプレッドシート認証ボタン

### 3.2 デザインガイドライン

```
カラースキーム:
- プライマリ: ピンク (#FF4D8E)
- セカンダリ: グレー (#F5F5F5)
- アクセント: ティール (#14B8A6)
- テキスト: ダークグレー (#333333)

フォント:
- 日本語: Noto Sans JP
- 英数字: Inter

コンポーネント:
- ボタン: 角丸、ホバー効果
- ドロップダウン: 検索機能付き (react-select)
- カード: シャドウ、ホバーで浮き上がる
- ステータスバッジ: 緑（アクティブ）、グレー（非アクティブ）
```

---

## 🗄️ 4. データベース設計

### 4.1 テーブル定義

```sql
-- ユーザー
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) UNIQUE NOT NULL,
  name VARCHAR(255),
  subscription_type VARCHAR(50) DEFAULT 'free',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- プラットフォーム認証情報（暗号化保存）
CREATE TABLE platform_authentications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  platform VARCHAR(50) NOT NULL,
  account_identifier VARCHAR(255), -- email, account ID, etc.
  encrypted_credentials TEXT NOT NULL, -- JSON暗号化
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, platform, account_identifier)
);

-- クローラー
CREATE TABLE crawlers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  platform VARCHAR(50) NOT NULL,
  status VARCHAR(20) DEFAULT 'inactive',
  
  spreadsheet_url TEXT NOT NULL,
  spreadsheet_id VARCHAR(255) NOT NULL,
  sheet_name VARCHAR(255) NOT NULL,
  
  account_ids TEXT[], -- 広告アカウントID配列
  report_config JSONB NOT NULL, -- ReportConfig
  schedule_config JSONB NOT NULL, -- ScheduleConfig
  
  tags TEXT[],
  
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  last_executed_at TIMESTAMP
);

-- ジョブ実行履歴
CREATE TABLE job_executions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  crawler_id UUID REFERENCES crawlers(id) ON DELETE CASCADE,
  status VARCHAR(20) NOT NULL, -- pending, running, completed, failed
  scheduled_at TIMESTAMP NOT NULL,
  started_at TIMESTAMP,
  completed_at TIMESTAMP,
  error_message TEXT,
  retry_count INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_crawlers_user_id ON crawlers(user_id);
CREATE INDEX idx_crawlers_status ON crawlers(status);
CREATE INDEX idx_job_executions_crawler_id ON job_executions(crawler_id);
CREATE INDEX idx_job_executions_status ON job_executions(status);
CREATE INDEX idx_job_executions_scheduled_at ON job_executions(scheduled_at);
```

---

## 🛠️ 5. 技術スタック推奨

### 5.1 フロントエンド

```typescript
// 推奨構成
Framework: Next.js 14+ (App Router)
Language: TypeScript
Styling: Tailwind CSS
UI Components: shadcn/ui または Chakra UI
State Management: Zustand または React Context
Form: React Hook Form + Zod
API Client: TanStack Query (React Query)
```

### 5.2 バックエンド

```typescript
// オプション1: Next.js API Routes（推奨、モノリス構成）
Framework: Next.js 14+
Language: TypeScript
ORM: Prisma
Validation: Zod

// オプション2: NestJS（マイクロサービス向け）
Framework: NestJS
Language: TypeScript
ORM: TypeORM or Prisma

// オプション3: FastAPI（Python、データ処理重視）
Framework: FastAPI
Language: Python 3.11+
ORM: SQLAlchemy
```

### 5.3 インフラ

```yaml
Database: PostgreSQL 15+ (Supabase, AWS RDS, Neon)
Cache/Queue: Redis (Upstash, AWS ElastiCache)
File Storage: AWS S3（ログ、エクスポート用）
Scheduler: 
  - BullMQ (Node.js)
  - Celery (Python)
  - AWS EventBridge (Serverless)

Hosting Options:
  Option 1: Vercel (Frontend) + AWS Lambda (Jobs)
  Option 2: AWS ECS/Fargate (Full control)
  Option 3: Docker Compose (On-premise)
```

### 5.4 認証・セキュリティ

```
認証:
- NextAuth.js (社内SSO連携可能)
- Auth0 (エンタープライズ向け)
- AWS Cognito

暗号化:
- 認証情報: AWS KMS or HashiCorp Vault
- 通信: TLS 1.3
- データベース: Encryption at rest

監視:
- Sentry (エラー追跡)
- Datadog / New Relic (APM)
- CloudWatch (AWS)
```

---

## 📊 6. API仕様（内部API）

### 6.1 認証エンドポイント

```
POST /api/auth/login
POST /api/auth/logout
GET  /api/auth/session
```

### 6.2 クローラーエンドポイント

```typescript
// クローラー一覧取得
GET /api/crawlers
Query: platform?, status?, tags[]
Response: Crawler[]

// クローラー作成
POST /api/crawlers
Body: CreateCrawlerDto
Response: Crawler

// クローラー詳細取得
GET /api/crawlers/:id
Response: Crawler

// クローラー更新
PATCH /api/crawlers/:id
Body: UpdateCrawlerDto
Response: Crawler

// クローラー削除
DELETE /api/crawlers/:id
Response: { success: boolean }

// クローラー複製
POST /api/crawlers/:id/duplicate
Response: Crawler

// クローラーテスト実行
POST /api/crawlers/:id/test
Response: { success: boolean, message?: string }
```

### 6.3 プラットフォーム認証エンドポイント

```typescript
// OAuth認証開始
GET /api/platforms/:platform/auth
Response: { auth_url: string }

// OAuthコールバック
GET /api/platforms/:platform/callback
Query: code, state
Response: { success: boolean }

// 認証済みアカウント一覧
GET /api/platforms/:platform/accounts
Response: Account[]

// 広告アカウント一覧
GET /api/platforms/:platform/ad-accounts
Query: account_id?
Response: AdAccount[]
```

### 6.4 スプレッドシートエンドポイント

```typescript
// Google認証開始
GET /api/spreadsheet/auth
Response: { auth_url: string }

// シート一覧取得
GET /api/spreadsheet/sheets
Query: spreadsheet_url
Response: Sheet[]
```

---

## 🔄 6.5 データ移行機能（エクスポート/インポート）

### 6.5.1 概要

既存の「カンタンデータクローラー」からの移行をサポートする機能。全クローラー設定を一括でエクスポート・インポート可能。

### 6.5.2 エクスポート機能

#### エクスポート可能なデータ

| カテゴリ | データ項目 | 移行可否 |
|---------|-----------|---------|
| 基本情報 | クローラー名 | ✅ |
| | プラットフォーム | ✅ |
| | ステータス | ✅ |
| スプレッドシート | URL | ✅ |
| | シートID | ✅ |
| | シート名 | ✅ |
| データソース | 広告アカウントID | ✅ (再認証必要) |
| レポート設定 | 期間設定タイプ | ✅ |
| | 開始日 | ✅ |
| | ディメンション | ✅ |
| | メトリクス | ✅ |
| | カスタムコンバージョン | ✅ |
| | カスタムイベント | ✅ |
| | 費用0除外設定 | ✅ |
| スケジュール | 更新頻度 | ✅ |
| | 実行時間 | ✅ |
| その他 | タグ | ✅ |
| 認証情報 | OAuthトークン | ❌ (セキュリティ上) |
| 履歴 | 実行履歴 | ❌ (新規記録) |

#### エクスポートデータ形式（JSON）

```json
{
  "export_version": "1.0",
  "exported_at": "2025-01-15T10:00:00Z",
  "total_crawlers": 158,
  "crawlers": [
    {
      "id": "af424905-c886-428c-98af-005df1e90ea7",
      "name": "アドコン_Meta",
      "platform": "meta_ads",
      "status": "active",
      "spreadsheet_url": "https://docs.google.com/spreadsheets/d/1XOFnQK5v9VjdMxgjIwdB8EGQswB7yZp8mmAh4wytUOo",
      "spreadsheet_id": "1XOFnQK5v9VjdMxgjIwdB8EGQswB7yZp8mmAh4wytUOo",
      "sheet_name": "Raw_Meta",
      "account_ids": [
        "641207518433024",
        "1777016949537683"
      ],
      "report_config": {
        "date_range_type": "from_specific_to_today",
        "date_range_start": "2025-08-01",
        "dimensions": ["date", "ad_set_name"],
        "metrics": [
          "spend",
          "impressions",
          "reach",
          "link_clicks",
          "website_registrations"
        ],
        "custom_conversions": [],
        "custom_events": [],
        "exclude_zero_cost": true
      },
      "schedule_config": {
        "frequency": "hourly",
        "execution_time": null
      },
      "tags": [],
      "created_at": "2025-02-06T10:00:00Z",
      "updated_at": "2025-11-14T05:00:00Z"
    }
  ]
}
```

#### エクスポートデータ形式（CSV）

```csv
id,name,platform,status,spreadsheet_url,spreadsheet_id,sheet_name,ad_account_ids,date_range_type,date_range_start,dimensions,metrics,exclude_zero_cost,frequency,tags
1,アドコン_Meta,meta_ads,active,https://docs.google.com/spreadsheets/d/xxx,xxx,Raw_Meta,"641207518433024,1777016949537683",from_specific_to_today,2025-08-01,"date,ad_set_name","spend,impressions,reach,link_clicks,website_registrations",true,hourly,""
```

### 6.5.3 インポート機能

#### インポートAPI

```typescript
// 一括インポート
POST /api/crawlers/import
Content-Type: multipart/form-data
Body: {
  file: File (JSON or CSV),
  options: {
    skip_duplicates: boolean,      // 重複スキップ
    update_existing: boolean,      // 既存を更新
    validate_only: boolean,        // バリデーションのみ（ドライラン）
    auto_activate: boolean         // インポート後に自動アクティブ化
  }
}
Response: {
  success: boolean,
  imported_count: number,
  skipped_count: number,
  errors: Array<{
    row: number,
    crawler_name: string,
    error: string
  }>,
  warnings: Array<{
    row: number,
    crawler_name: string,
    warning: string
  }>
}
```

#### インポートフロー

```
1. ファイルアップロード
   ↓
2. フォーマット検証（JSON/CSV）
   ↓
3. データバリデーション
   - 必須フィールドチェック
   - プラットフォーム値チェック
   - URL形式チェック
   ↓
4. プレビュー表示
   - インポート予定のクローラー一覧
   - エラー/警告の表示
   ↓
5. ユーザー確認
   ↓
6. データベース挿入
   ↓
7. 結果レポート表示
```

### 6.5.4 移行後の必要アクション

インポート完了後、以下の手動設定が必要：

1. **広告プラットフォーム再認証**
   - 各プラットフォームのOAuth認証を再実行
   - 広告アカウントIDは保持されるため、再選択不要

2. **Googleスプレッドシート再認証**
   - Google OAuth認証を実行
   - スプレッドシートURLは保持されるため、再入力不要

3. **テスト実行**
   - 各クローラーのテスト実行を推奨
   - エラーがないことを確認

### 6.5.5 UI設計

#### エクスポート画面

```
+------------------------------------------+
| データエクスポート                         |
+------------------------------------------+
| エクスポート形式:                          |
|   ○ JSON（推奨、完全データ）               |
|   ○ CSV（表計算ソフト互換）                |
|                                          |
| エクスポート対象:                          |
|   ☑ 全てのクローラー (158件)              |
|   ○ 選択したクローラーのみ                 |
|                                          |
| [エクスポート実行]                         |
+------------------------------------------+
```

#### インポート画面

```
+------------------------------------------+
| データインポート                           |
+------------------------------------------+
| ファイルをドラッグ＆ドロップ                |
| または [ファイルを選択]                    |
|                                          |
| 対応形式: JSON, CSV                       |
+------------------------------------------+
| オプション:                               |
|   ☐ 重複するクローラーをスキップ            |
|   ☐ 既存のクローラーを上書き更新            |
|   ☐ インポート後に自動でアクティブ化        |
+------------------------------------------+
| [プレビュー] [インポート実行]              |
+------------------------------------------+

↓ プレビュー後

+------------------------------------------+
| インポートプレビュー                       |
+------------------------------------------+
| ✅ 成功予定: 155件                        |
| ⚠️ 警告: 2件                             |
| ❌ エラー: 1件                            |
+------------------------------------------+
| エラー詳細:                               |
| Row 45: "テストクローラー"                 |
|   → プラットフォーム "unknown" は無効です   |
+------------------------------------------+
| [キャンセル] [インポート実行]              |
+------------------------------------------+
```

### 6.5.6 外部システムからの移行スクリプト

既存の「カンタンデータクローラー」からのエクスポート用ブラウザスクリプト：

```javascript
// Chrome Developer Consoleで実行
async function exportAllCrawlersWithDetails() {
  const pageData = JSON.parse(
    document.querySelector('script#__NEXT_DATA__').textContent
  );
  
  const userToken = pageData.props.pageProps.user.token;
  
  const crawlerLinks = Array.from(
    document.querySelectorAll('a[href^="/crawler/"]')
  );
  
  const crawlerIds = crawlerLinks.map(link => 
    link.getAttribute('href').replace('/crawler/', '')
  );
  
  console.log(`🔍 Found ${crawlerIds.length} crawlers`);
  
  const detailedCrawlers = [];
  
  for (let i = 0; i < crawlerIds.length; i++) {
    const crawlerId = crawlerIds[i];
    console.log(`📥 Fetching ${i+1}/${crawlerIds.length}: ${crawlerId}`);
    
    try {
      const response = await fetch(
        `https://app.easydatacrawler.com/api/crawlers/${crawlerId}`,
        {
          headers: {
            'Authorization': `Bearer ${userToken}`,
            'Content-Type': 'application/json'
          }
        }
      );
      
      if (response.ok) {
        const crawlerData = await response.json();
        detailedCrawlers.push(crawlerData);
      }
      
      await new Promise(resolve => setTimeout(resolve, 100));
      
    } catch (error) {
      console.error(`❌ Error fetching ${crawlerId}:`, error);
    }
  }
  
  // JSONエクスポート
  const exportData = {
    export_version: "1.0",
    exported_at: new Date().toISOString(),
    total_crawlers: detailedCrawlers.length,
    crawlers: detailedCrawlers
  };
  
  const jsonBlob = new Blob(
    [JSON.stringify(exportData, null, 2)], 
    { type: 'application/json' }
  );
  const jsonLink = document.createElement('a');
  jsonLink.href = URL.createObjectURL(jsonBlob);
  jsonLink.download = `crawlers_export_${new Date().toISOString().split('T')[0]}.json`;
  jsonLink.click();
  
  console.log(`✅ Export complete! ${detailedCrawlers.length} crawlers exported.`);
}

exportAllCrawlersWithDetails();
```

### 6.5.7 移行推奨フロー

```
Phase 1: エクスポート
├── 既存システムでスクリプト実行
├── JSON/CSVファイル取得
└── データ内容確認

Phase 2: インポート準備
├── 新システムにログイン
├── インポート画面を開く
└── ファイルをアップロード

Phase 3: バリデーション
├── プレビューでエラー確認
├── 必要に応じてデータ修正
└── 再アップロード

Phase 4: インポート実行
├── インポート実行
├── 結果レポート確認
└── エラーがあれば対応

Phase 5: 再認証
├── Googleスプレッドシート認証
├── 各広告プラットフォーム認証
└── アカウント紐付け確認

Phase 6: テスト
├── 主要クローラーのテスト実行
├── スプレッドシート出力確認
└── 本番稼働開始
```

---

## 🧪 7. テスト要件

### 7.1 単体テスト

```
カバレッジ目標: 80%以上

対象:
- ビジネスロジック
- データ変換処理
- バリデーション
- API クライアント

ツール:
- Jest (JavaScript/TypeScript)
- pytest (Python)
```

### 7.2 統合テスト

```
対象:
- API エンドポイント
- データベース操作
- 外部API連携（モック使用）

ツール:
- Supertest (Node.js)
- TestContainers (Database)
```

### 7.3 E2Eテスト

```
対象:
- クローラー作成フロー
- 認証フロー
- データ出力フロー

ツール:
- Playwright
- Cypress
```

---

## 📈 8. 非機能要件

### 8.1 パフォーマンス

```
レスポンスタイム:
- ページ読み込み: < 2秒
- API応答: < 500ms
- データ取得ジョブ: プラットフォームAPI次第

スループット:
- 同時クローラー実行: 100+ (水平スケール)
- API リクエスト: 1000 req/sec
```

### 8.2 可用性

```
目標稼働率: 99.5% (年間ダウンタイム < 44時間)

対策:
- ヘルスチェック
- 自動復旧
- 冗長構成（プロダクション）
```

### 8.3 スケーラビリティ

```
水平スケール:
- Webサーバー: ロードバランサー配下で複数インスタンス
- ワーカー: キューベースで動的スケール
- データベース: リードレプリカ

垂直スケール:
- 初期: t3.medium (2vCPU, 4GB RAM)
- スケール後: t3.xlarge (4vCPU, 16GB RAM)
```

### 8.4 セキュリティ

```
認証情報管理:
- AWS KMS / HashiCorp Vault で暗号化
- 環境変数での管理禁止
- ローテーション機能

通信:
- HTTPS強制
- CORS設定
- CSRFトークン

データ保護:
- データベース暗号化（at rest）
- バックアップ（日次）
- アクセスログ記録
```

---

## 🚀 9. 開発ロードマップ（詳細版）

### Phase 1: 基盤構築（Week 1-4）

**Week 1-2: 設計・環境構築**
- アーキテクチャ最終確定
- 技術選定完了
- 開発環境セットアップ
- CI/CD パイプライン構築
- データベース設計完了

**Week 3-4: 認証・基本UI**
- ユーザー認証実装
- ダッシュボード基本UI
- クローラー一覧表示
- データベースマイグレーション

### Phase 2: コア機能開発（Week 5-12）

**Week 5-6: Google Ads連携**
- Google Ads API クライアント実装
- OAuth認証フロー
- データ取得ロジック
- データ変換処理

**Week 7-8: スプレッドシート連携**
- Google Sheets API クライアント
- データ書き込みロジック
- エラーハンドリング
- リトライ機能

**Week 9-10: クローラー管理**
- クローラー作成フォーム（全6ステップ）
- バリデーション
- テスト実行機能
- 編集・削除機能

**Week 11-12: スケジューラー実装**
- ジョブキュー実装（BullMQ）
- スケジューラーエンジン
- ジョブ実行ロジック
- 実行履歴記録

### Phase 3: 機能拡張（Week 13-20）

**Week 13-14: Meta Ads連携**
- Meta Marketing API クライアント
- カスタムコンバージョン対応
- カスタムイベント対応

**Week 15-16: TikTok Ads連携**
- TikTok Ads API クライアント
- レポート取得ロジック

**Week 17-18: その他プラットフォーム**
- X Ads API
- LINE Ads API（API Key取得必要）

**Week 19-20: テスト・バグ修正**
- 単体テスト完了（カバレッジ80%）
- 統合テスト
- E2Eテスト
- バグ修正

### Phase 4: 本番リリース準備（Week 21-24）

**Week 21-22: パフォーマンス最適化**
- データベースクエリ最適化
- API レスポンスタイム改善
- キャッシュ戦略実装

**Week 23: セキュリティ監査**
- 脆弱性スキャン
- ペネトレーションテスト
- セキュリティ修正

**Week 24: ドキュメント・リリース**
- ユーザーマニュアル作成
- API ドキュメント整備
- 内部リリース
- フィードバック収集

---

## 💰 10. コスト試算（月次）

### 10.1 開発コスト（初期）

```
エンジニア:
- フロントエンド: 1名 × 6ヶ月
- バックエンド: 2名 × 6ヶ月
- インフラ: 0.5名 × 6ヶ月

外部サービス:
- 開発環境: $100/月 × 6ヶ月 = $600
```

### 10.2 運用コスト（月次、100ユーザー想定）

```
インフラ（AWS）:
- ECS Fargate: $300
- RDS PostgreSQL (db.t3.medium): $150
- ElastiCache Redis (cache.t3.micro): $50
- S3 + CloudWatch: $50
Total Infrastructure: $550/月

外部API:
- Google Ads API: 無料
- Meta Marketing API: 無料
- TikTok Ads API: 無料
（レート制限内の使用）

監視・ログ:
- Sentry: $26/月
- Datadog: $150/月
Total Monitoring: $176/月

合計: $726/月 ≈ ¥100,000/月
```

### 10.3 削減効果試算

```
外部ツール費用削減:
¥3,900 × 100人 × 12ヶ月 = ¥4,680,000/年

工数削減:
1日30分 × 100人 × 250日 = 12,500時間/年
時給¥3,000換算 = ¥37,500,000/年

合計削減効果: ¥42,180,000/年
投資回収期間: < 6ヶ月
```

---

## 📚 11. 参考ドキュメント

### 11.1 公式APIドキュメント

- **Google Ads API:** https://developers.google.com/google-ads/api/docs/start
- **Meta Marketing API:** https://developers.facebook.com/docs/marketing-apis
- **TikTok Ads API:** https://ads.tiktok.com/marketing_api/docs
- **Google Sheets API:** https://developers.google.com/sheets/api/guides/concepts
- **Twitter (X) Ads API:** https://developer.twitter.com/en/docs/ads/general/overview
- **LINE Ads API:** https://www.lycbiz.com/jp/service/line-ads/

### 11.2 ライブラリ・ツール

```
Google Ads API Client Libraries:
- Node.js: google-ads-api
- Python: google-ads-python

Meta Business SDK:
- Node.js: facebook-nodejs-business-sdk
- Python: facebook-business

Google Sheets:
- Node.js: googleapis
- Python: gspread
```

---

## ✅ 12. 開発チェックリスト

### 12.1 必須機能

- [ ] ユーザー認証・認可
- [ ] クローラー作成（6ステップ）
- [ ] Google Ads連携
- [ ] Meta Ads連携
- [ ] Googleスプレッドシート連携
- [ ] スケジューラー（日次・時間指定）
- [ ] ジョブ実行エンジン
- [ ] エラーハンドリング
- [ ] リトライ機能

### 12.2 推奨機能

- [ ] TikTok Ads連携
- [ ] X Ads連携
- [ ] クローラー複製機能
- [ ] タグ管理
- [ ] 実行履歴表示
- [ ] 通知機能（Slack/Email）
- [ ] データエクスポート機能（JSON/CSV）
- [ ] データインポート機能（一括移行）
- [ ] 外部システム移行スクリプト
- [ ] ダッシュボード（統計）

### 12.3 セキュリティ

- [ ] 認証情報の暗号化保存
- [ ] HTTPS強制
- [ ] CSRFトークン実装
- [ ] レート制限
- [ ] 監査ログ
- [ ] 脆弱性スキャン

### 12.4 テスト

- [ ] 単体テスト（カバレッジ80%+）
- [ ] 統合テスト
- [ ] E2Eテスト
- [ ] 負荷テスト
- [ ] セキュリティテスト

### 12.5 ドキュメント

- [ ] API仕様書
- [ ] ユーザーマニュアル
- [ ] 運用マニュアル
- [ ] アーキテクチャドキュメント
- [ ] データベーススキーマ

---

## 🎯 13. 成功の定義（KPI）

### 13.1 開発フェーズ

- ✓ 6ヶ月以内にMVPリリース
- ✓ テストカバレッジ80%以上
- ✓ 重大バグゼロでリリース
- ✓ API応答時間 < 500ms

### 13.2 運用フェーズ

- ✓ 稼働率99.5%以上
- ✓ ユーザー満足度80%以上
- ✓ データ取得成功率98%以上
- ✓ サポート問い合わせ < 10件/月

### 13.3 ビジネス効果

- ✓ 100ユーザー達成（6ヶ月後）
- ✓ 工数削減: 12,000時間/年
- ✓ 投資回収: < 6ヶ月

---

## 🚨 14. リスクと対策

### 14.1 技術リスク

**API仕様変更**
- リスク: プラットフォームがAPI仕様を変更
- 影響: データ取得失敗
- 対策:
  - APIバージョン管理
  - Deprecation通知の監視
  - フォールバック機能
  - 定期的な動作確認

**レート制限超過**
- リスク: API呼び出し制限に達する
- 影響: サービス停止
- 対策:
  - レート制限監視
  - キューイング
  - リクエスト最適化
  - プラン設計（アカウント数制限）

**データ整合性**
- リスク: 部分的な書き込み失敗
- 影響: データ不整合
- 対策:
  - トランザクション管理
  - ロールバック機能
  - データバリデーション
  - 整合性チェック

### 14.2 運用リスク

**スケーラビリティ**
- リスク: ユーザー増加時のパフォーマンス低下
- 影響: レスポンス遅延
- 対策:
  - 水平スケール設計
  - キャッシュ戦略
  - データベース最適化
  - 負荷テスト実施

**認証情報漏洩**
- リスク: 認証情報の不正アクセス
- 影響: セキュリティ侵害
- 対策:
  - 暗号化保存（KMS）
  - アクセス制御
  - 監査ログ
  - 定期的なセキュリティ監査

---

## 📞 15. サポート体制

### 15.1 ユーザーサポート

```
問い合わせチャネル:
- メール: support@company.com
- Slack: #data-crawler-support
- FAQ: 社内Wiki

対応時間:
- 平日9:00-18:00
- 緊急時: オンコール対応

SLA:
- 重大障害: 1時間以内に初動
- 通常問い合わせ: 24時間以内に回答
```

### 15.2 エスカレーションフロー

```
Level 1: 一般サポート担当
  ↓（解決できない場合）
Level 2: 開発チーム
  ↓（重大障害の場合）
Level 3: システム管理者・CTO
```

---

## 🎓 16. 学習リソース・研修計画

### 16.1 開発チーム向け

**Week 1:**
- システムアーキテクチャ理解
- 開発環境セットアップ
- コーディング規約確認

**Week 2:**
- 各広告プラットフォームAPI学習
- OAuth 2.0フロー理解
- セキュリティベストプラクティス

**Week 3-4:**
- 実装開始
- ペアプログラミング
- コードレビュー

### 16.2 ユーザー向け

**オンボーディング:**
- 初回ログイン時のチュートリアル
- クローラー作成ガイド（動画）
- FAQ・トラブルシューティング

**定期セミナー:**
- 月次: 新機能紹介
- 四半期: ベストプラクティス共有

---

## 📊 17. データモデル補足（JSON例）

### 17.1 ReportConfig例（Meta広告）

```json
{
  "date_range_type": "last_x_days_include",
  "lookback_days": 7,
  "dimensions": [
    "date",
    "campaign_name",
    "ad_set_name"
  ],
  "metrics": [
    "impressions",
    "clicks",
    "spend",
    "cpm",
    "ctr",
    "reach"
  ],
  "custom_conversions": [
    {
      "conversion_id": "1362683564373566"
    }
  ],
  "custom_events": [
    {
      "event_name": "Purchase"
    }
  ],
  "exclude_zero_cost": true
}
```

### 17.2 ScheduleConfig例

```json
{
  "frequency": "daily",
  "execution_time": "07:00",
  "timezone": "Asia/Tokyo"
}
```

### 17.3 暗号化された認証情報例（概念）

```json
{
  "platform": "meta_ads",
  "account_identifier": "user@example.com",
  "encrypted_credentials": "AQICAHh...（KMS暗号化データ）",
  "metadata": {
    "app_id": "123456789",
    "scopes": ["ads_read", "ads_management"]
  }
}
```

---

## 🔧 18. 環境設定ファイル例

### 18.1 .env.example

```bash
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/datacrawler

# Redis
REDIS_URL=redis://localhost:6379

# Google OAuth
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Meta OAuth
META_APP_ID=your-app-id
META_APP_SECRET=your-app-secret

# Encryption
KMS_KEY_ID=your-kms-key-id
ENCRYPTION_SECRET=your-secret-key

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
```

### 18.2 docker-compose.yml

```yaml
version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/datacrawler
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=datacrawler
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  worker:
    build: .
    command: npm run worker
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/datacrawler
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

volumes:
  postgres_data:
  redis_data:
```

---

## 📝 まとめ

この要件定義書には、カンタンデータクローラー社内版を開発するために必要な全ての情報が含まれています：

### ✅ 提供した情報

- **機能要件:** 全6ステップのクローラー作成フロー、詳細な設定項目
- **データモデル:** TypeScript型定義、データベーススキーマ
- **API仕様:** 内部API、外部プラットフォームAPI連携方法
- **UI/UX設計:** 画面一覧、レイアウト、デザインガイドライン
- **技術スタック:** 推奨技術、ライブラリ、インフラ構成
- **開発ロードマップ:** 24週間の詳細計画
- **コスト試算:** 開発・運用コスト、削減効果
- **リスク管理:** 技術・運用リスクと対策
- **テスト戦略:** 単体・統合・E2Eテスト
- **運用計画:** サポート体制、監視、SLA

### 🚀 次のアクション

1. **チーム編成:** フロントエンド1名、バックエンド2名、インフラ0.5名
2. **技術選定の最終決定:** Next.js + TypeScript + PostgreSQL + BullMQ 推奨
3. **開発環境構築:** Week 1から開始
4. **MVP目標:** 6ヶ月後に内部リリース

この要件定義書をベースに、すぐに開発を開始できます！
